export type PropTypes = {
    attributes?: any;
    id?: ( number | string );
    title: string;
    description: string;
}

export type AttributeTypes = {
    attributes: PropTypes;
    id: ( number | string );
}